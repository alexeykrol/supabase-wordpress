---
description: Запустить все тесты и проанализировать результаты
---

# АВТОМАТИЧЕСКОЕ ТЕСТИРОВАНИЕ

**Что делает эта команда:**
1. Запускает ВСЕ тесты автоматически (smoke + unit)
2. Собирает результаты в отчёт
3. Анализирует проблемы
4. Выдаёт bug report

**Твоя задача:**

## Шаг 1: Запустить все тесты

```bash
bash tests/run-all.sh
```

Этот скрипт автоматически:
- Запускает smoke tests (health check)
- Запускает unit tests (PHPUnit)
- Генерирует AI report
- Сохраняет результаты в `tests/reports/`

## Шаг 2: Проанализировать результаты

Прочитай отчёт и выдай пользователю:

**BUG REPORT FORMAT:**

```
🧪 TEST RESULTS
════════════════════════════════════════════════════════

Status: ✅ ALL PASSED / ❌ X FAILED

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 SMOKE TESTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Passed: 10/10
❌ Failed: 0

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 UNIT TESTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Passed: 30/30
❌ Failed: 0

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🐛 BUGS FOUND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
None / [List bugs here]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 RECOMMENDATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Suggestions for improvements]
```

## Шаг 3: Если есть ошибки

Подробно опиши:
- Какой тест упал
- Почему (expected vs actual)
- Где в коде проблема (file:line)
- Как исправить

## Если пользователь хочет НАПИСАТЬ новые тесты

Если пользователь спрашивает "напиши тест для X", помоги написать тесты для указанного кода.

**Стратегия тестирования:**

1. **Определи тип теста:**
   - Unit тест (функция, хук)
   - Component тест (UI компонент)
   - Integration тест (Server Action, API)
   - E2E тест (user flow)

2. **Определи что тестировать:**
   - Happy path (основной сценарий)
   - Edge cases (граничные случаи)
   - Error cases (обработка ошибок)
   - Различные входные данные

3. **Настрой окружение:**
   - Необходимые моки (Supabase, OpenAI, etc)
   - Test data
   - Helpers

4. **Напиши тесты:**
   - Arrange (подготовка)
   - Act (действие)
   - Assert (проверка)

5. **Покрытие:**
   - Все основные сценарии покрыты
   - Edge cases учтены
   - Ошибки обрабатываются

**Пример структуры:**

```typescript
describe('ComponentName', () => {
  describe('when user is authenticated', () => {
    it('should render correctly', () => {
      // test
    })

    it('should handle submit', () => {
      // test
    })
  })

  describe('when user is not authenticated', () => {
    it('should redirect to login', () => {
      // test
    })
  })

  describe('error handling', () => {
    it('should show error message on failure', () => {
      // test
    })
  })
})
```

**Best practices:**
- Тестируй поведение, не реализацию
- Используй понятные названия тестов
- Изолируй тесты друг от друга
- Мокируй внешние зависимости
- Проверяй как success, так и error cases

См. `.claude/testing-guide.md` для подробностей.
