# Release Protocol — WordPress Plugin

**Project:** Supabase Bridge
**Type:** WordPress Plugin

---

## ⚠️ CRITICAL: Always Follow This Protocol

**Проблема, которую решает этот протокол:**
- При "слепом" запуске build-release.sh можно забыть критические файлы
- Загрузка на продакшн без проверок может сломать сайт
- Нет проверки зависимостей перед релизом

**Этот протокол гарантирует:**
- ✅ Все зависимости проанализированы
- ✅ Все файлы включены в релиз
- ✅ Релиз протестирован перед публикацией

---

## 📋 Release Checklist

### Phase 1: Pre-Release Analysis (ОБЯЗАТЕЛЬНО!)

#### 1.1 Анализ зависимостей файлов

**Зачем:** Найти ВСЕ файлы, которые требует код плагина

```bash
# Найти все require/include в PHP коде
grep -rn "require\|include\|file_get_contents\|plugin_dir_path.*\\.html" supabase-bridge.php

# Найти упоминания .html файлов
grep -rn "\\.html" supabase-bridge.php

# Найти упоминания .sql файлов
grep -rn "\\.sql" supabase-bridge.php
```

**Результат:** Список ВСЕХ файлов, которые код пытается загрузить.

**Пример вывода:**
```
supabase-bridge.php:14:require __DIR__ . '/vendor/autoload.php';
supabase-bridge.php:915:$callback_path = plugin_dir_path(__FILE__) . 'test-no-elem-2-wordpress-paste.html';
supabase-bridge.php:1050:$form_path = plugin_dir_path(__FILE__) . 'auth-form.html';
```

**Action:** Записать все найденные файлы в список для проверки.

#### 1.2 Проверка build-release.sh

**Сравнить:** найденные файлы VS файлы в build-release.sh

```bash
# Что копирует build-release.sh?
grep "^cp " build-release.sh

# Что должен копировать (из анализа выше)?
# - supabase-bridge.php ✓
# - auth-form.html ✓
# - test-no-elem-2-wordpress-paste.html ← ПРОВЕРИТЬ!
# - vendor/ ✓
```

**Action:** Если файл найден в коде но НЕ в build-release.sh → ДОБАВИТЬ!

#### 1.3 Проверка критических файлов

**Обязательные файлы для WordPress плагина:**

```bash
# Проверить наличие
ls -lh supabase-bridge.php     # Главный файл плагина
ls -lh LICENSE                  # Лицензия
ls -lh README.md                # Документация
ls -lh CHANGELOG.md             # История версий
ls -lh vendor/                  # PHP зависимости
```

**Action:** Если файл отсутствует → СОЗДАТЬ или найти причину.

#### 1.4 Проверка версий в коде

**Где должна быть версия:**

```bash
# Проверить версию в главном файле плагина
grep "Version:" supabase-bridge.php

# Проверить версию в build-release.sh
grep "VERSION=" build-release.sh

# Проверить последнюю версию в CHANGELOG.md
head -20 CHANGELOG.md | grep "##"

# Проверить версию в README.md
grep "version-" README.md
```

**Action:** Если версии не совпадают → ОБНОВИТЬ перед релизом.

---

### Phase 2: Build Release

#### 2.1 Очистка старых сборок

```bash
# Удалить старый ZIP (если есть)
rm -f supabase-bridge-v*.zip

# Убедиться что build/ чист
rm -rf build/
```

#### 2.2 Запуск build-release.sh

```bash
bash build-release.sh
```

**Проверить вывод:**
- ✅ Нет ошибок "file not found"
- ✅ Размер ZIP разумный (не 0 байт, не слишком большой)
- ✅ Все предупреждения понятны (например, SQL файлы могут отсутствовать)

#### 2.3 Проверка содержимого ZIP

**КРИТИЧЕСКИ ВАЖНО:** Проверить ЧТО попало в архив

```bash
# Список ВСЕХ файлов
unzip -l supabase-bridge-v*.zip

# Проверить критические файлы
unzip -l supabase-bridge-v*.zip | grep "supabase-bridge.php"
unzip -l supabase-bridge-v*.zip | grep "auth-form.html"
unzip -l supabase-bridge-v*.zip | grep "test-no-elem-2-wordpress-paste.html"
unzip -l supabase-bridge-v*.zip | grep "vendor/"
```

**Action:** Если файл отсутствует в ZIP → ОСТАНОВИТЬ релиз, исправить build-release.sh.

---

### Phase 3: Pre-Production Testing

#### 3.1 Локальное тестирование (опционально)

Если есть локальный WordPress:
- Установить плагин из ZIP
- Активировать
- Проверить авторизацию (magic link, OAuth)

#### 3.2 Подготовка к продакшн деплою

```bash
# Проверить SSH доступ
source .production-credentials
ssh -i ~/.ssh/claude_prod_new -p $SSH_PORT $SSH_USER@$SSH_HOST "echo 'Connection OK'"
```

**Action:** Если SSH не работает → исправить доступ ПЕРЕД загрузкой.

---

### Phase 4: Production Deployment

#### 4.1 Бэкап текущей версии

```bash
# ВСЕГДА делать бэкап перед обновлением
source .production-credentials
ssh -i ~/.ssh/claude_prod_new -p $SSH_PORT $SSH_USER@$SSH_HOST \
  "cd $WP_PATH/wp-content/plugins && \
   cp -r supabase-bridge supabase-bridge.backup-$(date +%Y%m%d-%H%M%S)"
```

#### 4.2 Загрузка нового релиза

```bash
# Распаковать ZIP локально
unzip supabase-bridge-v*.zip -d /tmp/plugin-deploy

# Загрузить файлы на сервер
source .production-credentials
scp -i ~/.ssh/claude_prod_new -P $SSH_PORT -r \
  /tmp/plugin-deploy/supabase-bridge/* \
  $SSH_USER@$SSH_HOST:$WP_PATH/wp-content/plugins/supabase-bridge/

# Очистить временные файлы
rm -rf /tmp/plugin-deploy
```

#### 4.3 Проверка загрузки

```bash
# Проверить что все файлы загрузились
source .production-credentials
ssh -i ~/.ssh/claude_prod_new -p $SSH_PORT $SSH_USER@$SSH_HOST \
  "ls -lh $WP_PATH/wp-content/plugins/supabase-bridge/*.html"
```

**Action:** Если файлы отсутствуют → загрузить вручную.

---

### Phase 5: Post-Deployment Verification

#### 5.1 Проверка версии в WordPress

```bash
# Проверить версию в админке WordPress
# Или проверить файл напрямую
source .production-credentials
ssh -i ~/.ssh/claude_prod_new -p $SSH_PORT $SSH_USER@$SSH_HOST \
  "grep 'Version:' $WP_PATH/wp-content/plugins/supabase-bridge/supabase-bridge.php"
```

**Expected:** Version: X.Y.Z (новая версия)

#### 5.2 Функциональное тестирование

**Минимальный набор тестов:**

1. **Magic Link:** Отправить magic link, открыть, проверить callback
2. **OAuth:** Попробовать войти через Google/Apple (если настроено)
3. **Страница callback:** Открыть `/test-no-elem-2/` — не должно быть ошибки "file not found"

**Action:** Если что-то не работает → откатить на бэкап, искать проблему.

#### 5.3 Проверка логов

```bash
# Проверить нет ли ошибок в WordPress debug log
source .production-credentials
ssh -i ~/.ssh/claude_prod_new -p $SSH_PORT $SSH_USER@$SSH_HOST \
  "tail -50 $WP_PATH/wp-content/debug.log | grep 'Supabase Bridge'"
```

**Action:** Если есть ошибки → разобраться перед публикацией релиза.

---

### Phase 6: GitHub Release

#### 6.1 Проверка перед публикацией

```bash
# Еще раз проверить ZIP
unzip -t supabase-bridge-v*.zip
ls -lh supabase-bridge-v*.zip

# Проверить CHANGELOG.md
head -50 CHANGELOG.md
```

#### 6.2 Создание GitHub релиза

```bash
# Получить текущую версию
VERSION=$(grep "VERSION=" build-release.sh | head -1 | sed 's/VERSION="\(.*\)"/\1/')

# Создать релиз
gh release create "v$VERSION" \
  --title "v$VERSION" \
  --notes-file <(sed -n "/## \[$VERSION\]/,/## \[/p" CHANGELOG.md | head -n -1) \
  supabase-bridge-v$VERSION.zip
```

#### 6.3 Проверка релиза на GitHub

```bash
# Проверить что релиз создан
gh release view "v$VERSION"

# Проверить размер загруженного файла
gh release view "v$VERSION" --json assets --jq '.assets[] | "\(.name) - \(.size) bytes"'
```

---

## 🚨 Emergency Rollback

Если что-то пошло не так на продакшне:

```bash
# 1. Восстановить из бэкапа
source .production-credentials
BACKUP_DIR="supabase-bridge.backup-YYYYMMDD-HHMMSS"  # Найти нужный

ssh -i ~/.ssh/claude_prod_new -p $SSH_PORT $SSH_USER@$SSH_HOST \
  "cd $WP_PATH/wp-content/plugins && \
   rm -rf supabase-bridge && \
   cp -r $BACKUP_DIR supabase-bridge"

# 2. Проверить что сайт работает
# 3. Разобраться в проблеме локально
# 4. Исправить и повторить релиз
```

---

## 📊 Release Checklist Summary

Перед каждым релизом пройтись по чеклисту:

- [ ] **Phase 1.1:** Проанализированы зависимости файлов (grep по коду)
- [ ] **Phase 1.2:** build-release.sh содержит ВСЕ найденные файлы
- [ ] **Phase 1.3:** Все критические файлы существуют
- [ ] **Phase 1.4:** Версии синхронизированы во всех файлах
- [ ] **Phase 2.3:** ZIP проверен (unzip -l), все файлы на месте
- [ ] **Phase 3.2:** SSH доступ работает
- [ ] **Phase 4.1:** Бэкап создан на продакшне
- [ ] **Phase 4.3:** Файлы загружены на продакшн (ls проверка)
- [ ] **Phase 5.2:** Функциональные тесты пройдены (magic link работает)
- [ ] **Phase 5.3:** Нет ошибок в debug.log
- [ ] **Phase 6.3:** GitHub релиз создан, размер файла правильный

---

## 💡 Lessons Learned

### Incident: Missing callback handler (2025-12-24)

**Проблема:**
- Файл `test-no-elem-2-wordpress-paste.html` не включен в build-release.sh
- Релиз v0.9.10 выпущен без этого файла
- Загружен на продакшн → сайт сломался (magic link не работает)

**Root Cause:**
- Не был проведен анализ зависимостей (Phase 1.1)
- Не проверено содержимое ZIP (Phase 2.3)
- Не сделано функциональное тестирование (Phase 5.2)

**Fix:**
- Файл загружен вручную на продакшн
- build-release.sh исправлен
- Релиз пересобран и обновлен на GitHub

**Prevention:**
- ВСЕГДА выполнять Phase 1.1 (анализ зависимостей)
- ВСЕГДА проверять ZIP перед загрузкой (Phase 2.3)
- Добавлен этот протокол

---

**Last Updated:** 2025-12-24
**Created After Incident:** Missing callback handler file
