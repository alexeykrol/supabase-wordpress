# Agent Skills — Простое руководство

> **Для кого:** Разработчики, работающие с AI-ассистентами (Claude Code, GitHub Copilot)
> **Зачем:** Научить AI делать специализированные задачи лучше и дешевле
> **Источник:** https://code.visualstudio.com/docs/copilot/customization/agent-skills

---

## 🎈 Объяснение для школьника 10 лет

### Что это такое?

Представь, что у тебя есть робот-помощник (AI), который умеет программировать. Но он не знает **как именно ТЫ** хочешь работать в ТВОЕМ проекте.

**Agent Skills** — это как **учебник для робота**. Ты пишешь в него инструкции: "Когда я прошу создать commit, делай ВОТ ТАК", "Когда прошу написать тест, используй ВОТ ЭТОТ шаблон".

### Как это работает?

#### Без Skills (плохо):
```
Ты: "Создай git commit"
Робот: "Как именно? Какой формат сообщения?"
Ты: "Вот инструкция на 100 строк..." (каждый раз!)
Робот: "Ок, сделал"

↓ Много времени, много объяснений каждый раз
```

#### С Skills (хорошо):
```
Ты: "Создай git commit"
Робот: *читает skill commit-helper*
Робот: "Знаю! Делаю по инструкции из учебника"

↓ Один раз написал инструкцию — используй всегда!
```

### Когда это полезно?

✅ **Используй Skills, когда:**

1. **Повторяющиеся задачи** — ты часто просишь AI делать одно и то же
   - Пример: создавать commits, писать тесты, добавлять фичи

2. **Есть правила проекта** — в твоем проекте есть стандарты
   - Пример: "Все commit messages должны начинаться с `feat:` или `fix:`"

3. **Сложная процедура** — задача состоит из многих шагов
   - Пример: "Создать фичу = написать код + тесты + документацию"

4. **Нужны примеры/шаблоны** — робот должен использовать готовые файлы
   - Пример: шаблон теста, пример хорошего кода

❌ **НЕ нужны Skills, когда:**

1. Одноразовая задача — делаешь 1 раз и забыл
2. Простой вопрос — "Как работает эта функция?"
3. Стандартная задача — робот и так знает как делать

### Пример из жизни

**Без Skill:**
```
Ты: "Напиши тест для функции add()"
AI: "Какой фреймворк? Jest? Mocha? Vitest?"
Ты: "Vitest"
AI: "Какой стиль? describe/it или test()?"
Ты: "describe/it"
AI: "Arrange-Act-Assert или Given-When-Then?"
Ты: "AAA..."
AI: *наконец пишет тест*
```

**С Skill `testing`:**
```
Ты: "Напиши тест для функции add()"
AI: *читает skill testing*
    "Ага, этот проект использует Vitest,
     стиль describe/it, паттерн AAA,
     вот шаблон..."
AI: *сразу пишет правильный тест*
```

### Магия экономии

**Токены** — это "деньги" которые тратятся когда робот читает текст.

- Без Skills: Робот читает ВСЕ инструкции ВСЕГДА (дорого)
- С Skills: Робот читает только НУЖНУЮ инструкцию КОГДА НУЖНО (дешево)

**Аналогия:**
- Без Skills = носить ВСЮ библиотеку с собой
- С Skills = брать только НУЖНУЮ книгу с полки

---

## 🎯 Детальное руководство

### Что такое Agent Skills?

**Agent Skills** — это открытый стандарт для создания специализированных возможностей AI-ассистентов. Это папки с инструкциями, скриптами и ресурсами, которые AI загружает **только когда они нужны** для выполнения конкретной задачи.

**Ключевая особенность:** Прогрессивная загрузка (Progressive Disclosure) — AI узнает о skill постепенно, экономя токены.

### Структура Agent Skill

#### Базовая структура:
```
.github/skills/              # ← Рекомендуемое расположение
  └── skill-name/            # ← Папка для каждого навыка
      ├── SKILL.md           # ← Основной файл (обязательно!)
      ├── examples/          # ← Примеры (опционально)
      ├── templates/         # ← Шаблоны (опционально)
      └── scripts/           # ← Скрипты (опционально)
```

#### Формат SKILL.md:
```markdown
---
name: skill-name
description: Детальное описание что делает этот skill, когда его использовать и какие задачи он решает (до 1024 символов)
---

# Основные инструкции

Здесь подробные инструкции для AI...

## Шаг 1: ...
## Шаг 2: ...

## Примеры

См. `examples/example.md` для демонстрации.

## Шаблоны

Используй `templates/template.ts` как отправную точку.
```

**Frontmatter (обязательные поля):**
- `name` — идентификатор (lowercase с дефисами, макс 64 символа)
- `description` — детальное описание (макс 1024 символа)

### Прогрессивная загрузка (3 уровня)

AI загружает skill в 3 этапа:

```
📋 Уровень 1: Discovery (всегда загружено)
   ↓
   Только метаданные всех skills:
   - name: commit-helper
   - description: Создает git commit...

   Стоимость: ~50 токенов на skill
   Когда: При старте сессии

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 Уровень 2: Instructions (по требованию)
   ↓
   Полное содержимое SKILL.md

   Стоимость: ~500-1000 токенов
   Когда: Когда задача релевантна skill

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 Уровень 3: Resources (по запросу)
   ↓
   Дополнительные файлы (examples, templates)

   Стоимость: ~200-500 токенов на файл
   Когда: Явно упомянуты в инструкциях
```

**Экономия:** Если у тебя 20 skills, но используешь только 2 — платишь за 2, а не за 20!

### Сравнение с другими подходами

| Аспект | Agent Skills | Custom Instructions | .claude/commands/ |
|--------|--------------|---------------------|-------------------|
| **Загрузка** | По требованию | Всегда активны | По вызову |
| **Ресурсы** | Инструкции + файлы | Только текст | Только текст |
| **Платформы** | Кросс-платформенные | VS Code only | Claude Code only |
| **Токены** | Экономичные | Тратятся всегда | Средние |
| **Формат** | Стандартизированный | Произвольный | Свой формат |
| **Композиция** | Да (несколько skills) | Нет | Частично |

---

## 🛠️ Практическое применение

### Пример 1: Skill для коммитов

**Задача:** Научить AI создавать commits по твоему стандарту.

#### Структура:
```
.github/skills/commit-helper/
  ├── SKILL.md
  ├── examples/
  │   ├── good-commits.md        # Примеры хороших сообщений
  │   └── bad-commits.md         # Антипаттерны
  └── templates/
      └── commit-message.txt     # Шаблон сообщения
```

#### SKILL.md:
```markdown
---
name: commit-helper
description: Создает git commit с правильным сообщением по Conventional Commits стандарту. Анализирует изменения, проверяет отсутствие секретов в коде, форматирует commit message. Используй когда пользователь просит "создать commit", "закоммитить", "сохранить изменения".
---

# Процесс создания commit

## 1. Анализ изменений

Выполни параллельно:
```bash
git status              # Все измененные файлы
git diff                # Unstaged изменения
git diff --staged       # Staged изменения
git log --oneline -n 5  # Стиль последних коммитов
```

## 2. Проверка безопасности

❌ НЕ коммитить:
- `.env`, `.env.local` (секреты!)
- `node_modules/`
- Файлы с паролями/токенами
- Build артефакты (dist/, build/)

⚠️ Если пользователь настаивает — предупреди о рисках!

## 3. Формат commit message

См. `templates/commit-message.txt` для шаблона.
См. `examples/good-commits.md` для примеров.

**Conventional Commits:**
```
<type>: <краткое описание>

<детали ПОЧЕМУ, не ЧТО>

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

**Types:**
- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — документация
- `refactor:` — рефакторинг без изменения поведения
- `test:` — добавление тестов
- `chore:` — техническая работа

## 4. Создание commit

```bash
git add file1.ts file2.ts  # Конкретные файлы
git commit -m "..."        # Используй шаблон
```
```

#### templates/commit-message.txt:
```
feat: Add user authentication

Implemented JWT-based authentication system because
the app needs secure user sessions.

Key changes:
- Login/logout endpoints
- JWT token generation
- Session middleware

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

#### examples/good-commits.md:
```markdown
# Примеры хороших commit messages

## Фича
```
feat: Add dark mode toggle

Users requested dark mode support. Implemented theme
switcher in settings with localStorage persistence.
```

## Баг фикс
```
fix: Resolve race condition in auth callback

Magic link authentication sometimes failed due to duplicate
callbacks. Added distributed lock with GET_LOCK().
```

## Рефакторинг
```
refactor: Extract validation functions

Moved input validation into separate module for reusability.
No behavior changes, just code organization.
```
```

### Пример 2: Skill для тестирования

**Задача:** Стандартизировать написание тестов.

#### Структура:
```
.github/skills/testing/
  ├── SKILL.md
  ├── templates/
  │   ├── unit-test.ts           # Шаблон unit теста
  │   ├── integration-test.ts    # Шаблон integration теста
  │   └── e2e-test.ts            # Шаблон E2E теста
  └── examples/
      └── best-practices.md      # Best practices
```

#### SKILL.md:
```markdown
---
name: testing
description: Comprehensive testing workflow for TypeScript/JavaScript projects. Provides templates for unit, integration, and E2E tests using Vitest/Jest. Includes best practices, AAA pattern, test naming conventions. Use when creating or reviewing tests.
---

# Testing Workflow

## 1. Определи тип теста

**Unit test:**
- Одна функция/компонент в изоляции
- Моки для всех зависимостей
- Быстрые (~1-10ms)

**Integration test:**
- Несколько компонентов вместе
- Моки только для внешних сервисов
- Средние (~10-100ms)

**E2E test:**
- Полный user flow
- Реальный браузер
- Медленные (~1-10s)

## 2. Используй шаблон

- Unit: `templates/unit-test.ts`
- Integration: `templates/integration-test.ts`
- E2E: `templates/e2e-test.ts`

## 3. Паттерн AAA

```typescript
// Arrange — подготовка
const input = ...;

// Act — действие
const result = functionName(input);

// Assert — проверка
expect(result).toBe(expected);
```

## 4. Naming Convention

```typescript
describe('FunctionName', () => {
  it('should return X when Y', () => {
    // Test
  });

  it('should throw error when invalid input', () => {
    // Test
  });
});
```

## 5. Best Practices

См. `examples/best-practices.md`
```

#### templates/unit-test.ts:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { functionName } from './module';

describe('functionName', () => {
  it('should do something when given valid input', () => {
    // Arrange
    const input = 'test';
    const expected = 'result';

    // Act
    const result = functionName(input);

    // Assert
    expect(result).toBe(expected);
  });

  it('should throw error when given invalid input', () => {
    // Arrange
    const invalidInput = null;

    // Act & Assert
    expect(() => functionName(invalidInput)).toThrow('Invalid input');
  });

  it('should handle edge case', () => {
    // Arrange
    const edgeCase = '';

    // Act
    const result = functionName(edgeCase);

    // Assert
    expect(result).toBe('');
  });
});
```

### Пример 3: Skill для фичи

**Задача:** Процесс добавления новой функциональности.

```
.github/skills/feature-development/
  ├── SKILL.md
  ├── templates/
  │   ├── component.tsx          # Шаблон компонента
  │   ├── hook.ts                # Шаблон кастомного хука
  │   └── api-endpoint.ts        # Шаблон API route
  └── checklists/
      └── feature-checklist.md   # Чеклист готовности фичи
```

#### SKILL.md:
```markdown
---
name: feature-development
description: Complete workflow for developing new features. Includes planning, implementation, testing, and documentation. Provides templates for components, hooks, API endpoints. Use when user requests "add feature", "create new functionality", "implement X".
---

# Feature Development Process

## 1. Планирование

**Вопросы пользователю:**
- Какую проблему решает фича?
- Кто будет использовать?
- Какие edge cases?
- Есть ли дизайн/макет?

## 2. Разбиение на задачи

Используй TodoWrite для трекинга:
- [ ] Создать компонент UI
- [ ] Реализовать бизнес-логику
- [ ] Добавить API endpoint (если нужно)
- [ ] Написать тесты
- [ ] Обновить документацию

## 3. Реализация

**Шаблоны:**
- React компонент: `templates/component.tsx`
- Custom hook: `templates/hook.ts`
- API endpoint: `templates/api-endpoint.ts`

**Порядок:**
1. API/backend (если нужно)
2. Business logic (hooks, services)
3. UI components
4. Integration
5. Tests

## 4. Тестирование

- Unit тесты для логики
- Integration тесты для API
- E2E тесты для критичных флоу

## 5. Готовность

Чеклист: `checklists/feature-checklist.md`

- [ ] Код написан
- [ ] Тесты проходят
- [ ] Документация обновлена
- [ ] Edge cases покрыты
- [ ] Error handling добавлен
- [ ] Code review пройден
```

---

## 📊 Когда использовать Agent Skills

### ✅ Используй Skills для:

| Сценарий | Пример | Почему Skills? |
|----------|--------|----------------|
| **Повторяющиеся процессы** | Создание commits, PR, releases | Один раз опиши — используй всегда |
| **Стандарты проекта** | Code style, naming conventions | Консистентность во всем проекте |
| **Сложные workflow** | Feature development, debugging | Многоэтапные процедуры с шагами |
| **Шаблоны кода** | Tests, components, API endpoints | Готовые примеры ускоряют работу |
| **Специфичная логика** | Deployment, database migrations | Уникальные процессы твоего проекта |

### ❌ НЕ используй Skills для:

| Сценарий | Почему не нужно | Альтернатива |
|----------|-----------------|--------------|
| Одноразовые задачи | Не окупится время на создание | Обычный промпт |
| Общие вопросы | AI и так знает | Просто спроси |
| Простые задачи | Overengineering | Короткая инструкция |
| Нестандартные кейсы | Skills для типовых задач | Custom prompt |

---

## 🚀 Как начать использовать

### Шаг 1: Создать структуру

```bash
# Создать директорию для skills
mkdir -p .github/skills

# Проверить что создалось
ls -la .github/
```

### Шаг 2: Создать первый skill

Начни с чего-то простого и часто используемого — например, commits:

```bash
# Создать папку для skill
mkdir -p .github/skills/commit-helper/{templates,examples}

# Создать основной файл
cat > .github/skills/commit-helper/SKILL.md <<'EOF'
---
name: commit-helper
description: Создает git commit с правильным сообщением по Conventional Commits стандарту. Анализирует изменения и форматирует commit message.
---

# Процесс создания commit

## 1. Анализ изменений
- git status
- git diff

## 2. Формат сообщения
```
<type>: <description>

<details>

🤖 Generated with Claude Code
```

## Types
- feat: новая функциональность
- fix: исправление бага
- docs: документация
- refactor: рефакторинг
- test: тесты
- chore: техническая работа
EOF

# Добавить шаблон
cat > .github/skills/commit-helper/templates/message.txt <<'EOF'
feat: Add feature name

Detailed explanation of WHY this change was needed.

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
EOF
```

### Шаг 3: Использовать skill

```bash
# Попроси AI использовать skill
# Prompt: "Создай commit используя commit-helper skill"
```

AI автоматически:
1. Обнаружит skill (Discovery)
2. Загрузит SKILL.md (Instructions)
3. Использует шаблон (Resources)
4. Создаст правильный commit

### Шаг 4: Расширить коллекцию

Добавь еще skills по мере надобности:

```bash
# Testing skill
mkdir -p .github/skills/testing

# Feature development skill
mkdir -p .github/skills/feature-development

# Code review skill
mkdir -p .github/skills/code-review
```

---

## 💡 Best Practices

### 1. Один skill — одна задача

❌ **Плохо:**
```
.github/skills/everything/
  └── SKILL.md   # 1000 строк про все на свете
```

✅ **Хорошо:**
```
.github/skills/
  ├── commit-helper/      # Только commits
  ├── testing/            # Только тесты
  └── feature-dev/        # Только фичи
```

### 2. Детальные описания

❌ **Плохо:**
```yaml
name: commit
description: Creates commits
```

✅ **Хорошо:**
```yaml
name: commit-helper
description: Создает git commit с правильным сообщением по Conventional Commits стандарту. Анализирует staged изменения, проверяет отсутствие секретов, форматирует message. Используй когда нужно закоммитить изменения.
```

**Почему:** AI решает использовать ли skill на основе description!

### 3. Структурируй инструкции

❌ **Плохо:**
```markdown
Сделай commit. Проверь файлы. Напиши сообщение. Не забудь секреты...
```

✅ **Хорошо:**
```markdown
# Процесс создания commit

## 1. Анализ изменений
- git status
- git diff

## 2. Проверка безопасности
- ❌ НЕ коммитить .env

## 3. Формат сообщения
...
```

### 4. Используй относительные пути

❌ **Плохо:**
```markdown
См. /Users/me/project/.github/skills/testing/template.ts
```

✅ **Хорошо:**
```markdown
См. `templates/template.ts` для примера.
```

### 5. Добавляй примеры

```
.github/skills/testing/
  ├── SKILL.md
  ├── templates/
  │   └── unit-test.ts        ← Шаблон (как надо)
  └── examples/
      ├── good-test.md        ← Хорошие примеры
      └── bad-test.md         ← Антипаттерны (как НЕ надо)
```

Примеры помогают AI понять контекст лучше, чем описание.

---

## 🔄 Миграция с .claude/commands/

Если у тебя уже есть команды в `.claude/commands/`, можешь:

### Вариант 1: Оставить как есть

```
.claude/commands/           ← Оставить для простых команд
  ├── commit.md
  └── fix.md

.github/skills/             ← Добавить для сложных skills
  └── testing/
      ├── SKILL.md
      └── templates/
```

**Когда:** Текущая система работает, не хочешь переделывать.

### Вариант 2: Полная миграция

```bash
# Для каждой команды
for cmd in .claude/commands/*.md; do
  name=$(basename "$cmd" .md)
  mkdir -p ".github/skills/$name"

  # Конвертировать формат
  # (нужно вручную добавить frontmatter)
  cp "$cmd" ".github/skills/$name/SKILL.md"
done
```

**Когда:** Хочешь кросс-платформенность и прогрессивную загрузку.

### Вариант 3: Гибрид

```
.claude/commands/           ← Команды без ресурсов
  ├── explain.md
  └── review.md

.github/skills/             ← Skills с ресурсами
  ├── testing/
  │   ├── SKILL.md
  │   └── templates/
  └── feature-dev/
      ├── SKILL.md
      └── examples/
```

**Когда:** Лучшее из двух миров.

---

## 📦 Готовые Skills из сообщества

### GitHub: awesome-copilot

```bash
# Посмотреть примеры
open https://github.com/github/awesome-copilot
```

Содержит:
- Готовые skills от сообщества
- Примеры для разных языков
- Best practices

### Anthropic: skills repository

```bash
# Референсные реализации от Anthropic
open https://github.com/anthropics/skills
```

Содержит:
- Официальные примеры
- Документация по формату
- Шаблоны для старта

---

## 🎓 Практические упражнения

### Упражнение 1: Создай skill для коммитов

1. Создай `.github/skills/commit-helper/`
2. Напиши SKILL.md с инструкциями
3. Добавь шаблон в `templates/`
4. Протестируй на реальном commit

### Упражнение 2: Создай skill для тестирования

1. Создай `.github/skills/testing/`
2. Добавь 3 шаблона (unit, integration, e2e)
3. Напиши best practices в `examples/`
4. Попроси AI написать тест используя skill

### Упражнение 3: Композиция skills

Попроси AI:
> "Создай новую фичу с тестами и задокументируй её"

AI должен использовать 3 skills одновременно:
- `feature-development` — как создавать фичи
- `testing` — как писать тесты
- `documentation` — как документировать

---

## 🐛 Частые ошибки

### Ошибка 1: Слишком общее описание

❌ **Проблема:**
```yaml
description: Helps with coding
```

AI не понимает КОГДА использовать skill.

✅ **Решение:**
```yaml
description: Creates git commits using Conventional Commits standard. Analyzes changes, checks for secrets, formats commit message. Use when user asks to commit, save changes, or create commit.
```

### Ошибка 2: Абсолютные пути

❌ **Проблема:**
```markdown
See /Users/me/project/template.ts
```

Не работает на других машинах.

✅ **Решение:**
```markdown
См. `templates/template.ts`
```

### Ошибка 3: Один огромный skill

❌ **Проблема:**
```
.github/skills/dev-helper/
  └── SKILL.md   # 2000 строк про ВСЁ
```

Тратит много токенов, AI путается.

✅ **Решение:**
```
.github/skills/
  ├── commit/
  ├── testing/
  ├── feature/
  └── review/
```

Каждый skill фокусируется на одной задаче.

---

## 📈 Измерение эффективности

### До Agent Skills:

```
Задача: Создать 5 commits за сессию

Токены на каждый commit:
- Загрузка всех команд: 500 токенов
- Выполнение: 200 токенов
Итого: 700 × 5 = 3500 токенов

Время: ~5 минут на commit (объяснения)
Итого: 25 минут
```

### После Agent Skills:

```
Задача: Создать 5 commits за сессию

Токены:
- Discovery (все skills): 200 токенов (1 раз)
- Instructions (commit-helper): 500 токенов (1 раз)
- Выполнение каждого: 200 токенов × 5
Итого: 200 + 500 + 1000 = 1700 токенов

Экономия: 1800 токенов (51%)

Время: ~2 минуты на commit (без объяснений)
Итого: 10 минут

Экономия: 15 минут (60%)
```

---

## 🎯 Рекомендации по внедрению

### Неделя 1: Эксперимент

- Создай 1-2 простых skills (commit, testing)
- Попробуй использовать в реальной работе
- Оцени удобство и экономию

### Неделя 2-3: Расширение

- Добавь еще 3-5 skills для частых задач
- Feature development
- Code review
- Documentation
- Debugging

### Месяц 1+: Оптимизация

- Собери статистику: какие skills используются чаще
- Улучши описания на основе опыта
- Добавь недостающие примеры/шаблоны
- Удали неиспользуемые skills

---

## 🤝 Как поделиться этим руководством

Этот файл создан чтобы им можно было поделиться:

### С командой:
```bash
# Отправь файл коллегам
cp AGENT_SKILLS_GUIDE.md ~/Desktop/
```

### В репозитории:
```bash
# Закоммить в проект
git add AGENT_SKILLS_GUIDE.md
git commit -m "docs: Add Agent Skills guide"
git push
```

### Публично:
- Опубликуй на GitHub Gist
- Поделись в Twitter/LinkedIn
- Добавь в блог

---

## 📚 Дополнительные ресурсы

- **Официальная документация:** https://code.visualstudio.com/docs/copilot/customization/agent-skills
- **Примеры от сообщества:** https://github.com/github/awesome-copilot
- **Anthropic Skills:** https://github.com/anthropics/skills
- **VS Code Insiders:** https://code.visualstudio.com/insiders/ (для ранней поддержки)

---

## ❓ FAQ

**Q: Работают ли Skills в Claude Code?**
A: Да, если реализована поддержка стандарта. Проверь документацию Claude Code.

**Q: Сколько skills можно создать?**
A: Неограниченно, но рекомендуется 10-20 для баланса между функциональностью и удобством.

**Q: Можно ли использовать TypeScript/Python в skills?**
A: Да, skills могут содержать любые файлы — скрипты, шаблоны, примеры кода.

**Q: Как AI выбирает какой skill использовать?**
A: На основе `description` в frontmatter — поэтому важно писать детальные описания.

**Q: Нужно ли коммитить skills в git?**
A: Да! Skills — часть проекта, их надо версионировать как код.

**Q: Чем отличается от GitHub Actions/CI?**
A: Skills — для AI-ассистентов (помощь в разработке), Actions — для автоматизации (CI/CD pipeline).

---

## ✅ Чеклист готовности к использованию

- [ ] Прочитал руководство полностью
- [ ] Понял концепцию прогрессивной загрузки
- [ ] Создал `.github/skills/` директорию
- [ ] Создал первый skill (например, commit-helper)
- [ ] Протестировал skill на реальной задаче
- [ ] Оценил экономию токенов и времени
- [ ] Запланировал еще 2-3 skills для частых задач
- [ ] Поделился руководством с командой (опционально)

---

**Версия:** 1.0
**Дата:** 2025-12-19
**Автор:** Создано с помощью Claude Code
**Лицензия:** Свободное использование и распространение
